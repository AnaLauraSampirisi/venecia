
import { Flex } from "@chakra-ui/react"

const CartWidget = ({}) => {
    return (
        <>
        <Flex>
            <p className="cantidad_items">{1}</p>
        </Flex>
        </>
    )
}

export default CartWidget